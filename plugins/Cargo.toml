[package]
name = "zeta-plugins"
version = "0.1.0"
authors = ["Mikkel Kroman <mk@uplink.io>"]

[dependencies]
zeta = { version = "*", path = ".." }
log = "0.3"
scraper = "0.4"
hyper = "0.9"

[lib]
crate-type = ["dylib"]
